{
  "name": "loreweaver",
  "version": "0.1.0",
  "private": true,
  "description": "AI-native persistent storytelling and memory platform",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "docker compose up -d --build",
    "down": "docker compose down",
    "logs": "docker compose logs -f api web",
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "node node_modules/vitest/vitest.mjs run --config apps/api/vitest.config.ts && node node_modules/vitest/vitest.mjs run --config apps/web/vitest.config.ts",
    "test:e2e": "npx playwright test --config apps/web/playwright.config.ts",
    "smoke": "npx tsx scripts/smoke-test.ts",
    "verify": "npx tsx scripts/verify.ts",
    "clean": "rm -rf apps/*/node_modules apps/*/dist packages/*/node_modules packages/*/dist node_modules",
    "seed": "docker compose exec api node dist/seed/index.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.8.0",
    "jest": "^30.4.2",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "overrides": {
    "fast-uri": "^3.1.5",
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2"
  }
}
